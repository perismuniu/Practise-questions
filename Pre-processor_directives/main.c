#include <stdio.h>
#include "main.h"

/**
 *main - Entry point.
 *Compute Arithmetic functions - Sum, sub, mult, div, mod
 *Return : Always (0)
 */

int main(void)
{
printf("sum = %d\n", sum(100, 50));
printf("sub = %d\n", sub(100, 50));
printf("mult = %d\n", mult(100, 50));
printf("div = %d\n", div(100, 50));
printf("mod = %d\n", mod(100, 50));

return (0);
}
